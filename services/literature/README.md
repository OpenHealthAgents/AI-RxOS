# literature

Part of the AI-RxOS platform. See `/architecture` at the repo root for the
full service contract this implements. Runs on port **8082**.

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8082
```
